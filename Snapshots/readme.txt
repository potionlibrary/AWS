Deletes old snapshots 

Below script iterates through list of snapshots and attempts delete one of them per cycle.

Queries are based on date so any snapshot > older then X date will be deleted. if you run the script the date variable has to be changed to delete snapshots for the month you want.
